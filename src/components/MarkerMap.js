import { Cte } from "./Icon";

export const MarkerMap = () => {
  return (
    <div
      className="text-gray-500 bg-gray-200 rounded-lg"
      //   onClick={(e) => console.log(e)}
    >
      <Cte />
    </div>
  );
};
